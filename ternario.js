const minimunAge = 18;
const clientAge = 16;

if (clientAge >= minimunAge){
    console.log("over 18 years old")
} else{
    console.log("under 18 years old")
}

              //condicion                 //true                 //false
console.log(clientAge >= minimunAge ? "over 18 years old" : "under 18 years old")