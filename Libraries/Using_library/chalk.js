import chalk from 'chalk'; // to change the way to import from requite to import ( import is the most up-to-date way to import) we need to add a this line in the packege.json '"type":"module"'


console.log(chalk.blue('Hello world!'));